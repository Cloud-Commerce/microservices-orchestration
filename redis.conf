# Enable persistence
appendonly yes
appendfsync everysec

# Secure Redis (optional)
# requirepass mypassword
# user myuser +@all ~* >mypassword  # ACL rule

# Limit memory usage (adjust as needed)
maxmemory 256mb
maxmemory-policy allkeys-lru